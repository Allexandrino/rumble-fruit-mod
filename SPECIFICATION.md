# Спецификация поведения (black-box)

Этот документ — **контракт системы**: как она обязана себя вести снаружи.
Источник правды — тесты (`src/test`). Если код и спецификация расходятся,
тесты падают. Правишь спецификацию → прогоняешь:

```
gradlew test pitest
```

Красное = код не соответствует спецификации. Зелёное = соответствует.

Обозначения: тик = 1/20 секунды. Позиции в блоках. Углы в градусах.

---

## 1. Пул орбов (`OrbPool`)

У игрока есть пул электро-орбов. Скиллы Z/V тратят их.

| Правило | Спецификация |
|---|---|
| Максимум | `MAX_ORBS = 4` |
| Старт | пул полон (4/4) |
| Период перезарядки | `RECHARGE_TICKS = 100` тиков (5 с) |

**Правила:**
1. `consume(n, now)` забирает `min(n, доступно)` орбов и возвращает сколько забрал. Пул не уходит в минус.
2. При первом расходе (пул стал неполным) назначается перезарядка на `now + 100`.
3. `tick(now)` восстанавливает ровно 1 орб, если наступил назначенный тик. Возвращает `true`, если восстановил.
4. Следующая перезарядка — через 100 тиков после ТЕКУЩЕЙ (не немедленно). Пример: потратил 3 на тике 0 → +1 на 100, +1 на 200, +1 на 300, на 400 — ничего (пул полон).
5. На полном пуле перезарядка не назначается и не срабатывает.
6. Повторный расход НЕ сдвигает уже назначенную перезарядку.

## 2. Серверные орбы (`OrbManager`)

1. `consume(player, n)` — то же, что в пуле + рассылка клиентам `OrbSyncPacket` (ровно 1 пакет на вызов).
2. `onPlayerTick` реагирует только на `ServerPlayer`; при срабатывании перезарядки — рассылка `OrbSyncPacket`.
3. `onLogout` удаляет пул игрока (свежий вход = полный пул).

## 3. Кулдауны (`Cooldowns`)

1. `fraction(lastUse, now, cd)` = `clamp(1 - (now - lastUse)/cd, 0, 1)`. Сразу после применения = 1.0, на половине = 0.5, после = 0.0.
2. `secondsLeft(lastUse, now, cd)` = `ceil((cd - elapsed) / 20)` — округление вверх (59 тиков → 1с, 60 → 0с).

## 4. Уровни зарядки (`ChargeLevels`)

| Удержание | Уровень |
|---|---|
| < 5 тиков | 1 |
| 5–14 | 2 |
| 15–29 | 3 |
| ≥ 30 | 4 |

## 5. Кривые анимации (`AnimCurves`)

1. `phase(t, start, end)` = `clamp((t - start)/(end - start), 0, 1)`.
2. `easeInOut(k)` = `k²·(3 − 2k)` (smoothstep: 0→0, 0.5→0.5, 1→1, монотонна).
3. `lerp(a, b, k)` = `a + (b − a)·k`.
4. `clamp(v, min, max)` = `max(min, min(max, v))`.

## 6. Вектор (`Vec`)

Стандартная 3D-математика: add/subtract/scale/dot/length/distanceTo/lerp.
`normalize()`: нулевой вектор → (0,0,0), иначе деление на длину (результат — единичный).

## 7. Камера (`CameraMath`)

1. `lookYaw(δ)` = `atan2(−δx, δz)` в градусах. Юг(+Z)=0, восток(+X)=−90, запад=+90, север=±180.
2. `lookPitch(δ)` = `−atan2(δy, √δx²+δz²)`. Цель снизу → положительный питч (вниз), строго вниз = +90.
3. `bankRoll(vel, yaw)` = `clamp(−(vx·cos yaw + vz·sin yaw) · 14, −7, +7)`.
4. `glide(cur, target, k)` = линейная интерполяция позиции камеры.

## 8. Молния-луч (`RayPolyline`)

1. `generate(from, yaw, pitch, length, random)` → список точек ломаного луча.
2. 14 сегментов по `length/14` каждый; направление дрожит (джиттер ±0.55/сегмент).
3. Посередине (i=7) с шансом 50% — ответвление длиной 0.4·length (рекурсивно).
4. Детерминизм: одинаковый seed → одинаковые точки.
5. Первая точка — в пределах 1 блока от `from`.

## 9. Ульта J (`ReleaseSkill`)

### Фаза зарядки (60 тиков)
1. Игрок **неуязвим** (Resistance 4 + Glowing на всю зарядку).
2. Подъём по фазам вертикальной скорости: тики 1–7 = **0.12**, 8–51 = **0.48**, 52–60 = **0** (зависание, ~30 блоков суммарно).
3. Горизонтальная скорость давится до 40%.
4. Молнии выходят **из рук и ног** (конечности подключаются: тик 1+, 15+, 30+, 45+).
5. Молния с неба бьёт в кастера каждые 4 тика.
6. Оболочка из искр сжимается на тело, густеет каждый тик.
7. Клиентам шлётся поза вознесения (CombatAnimPacket, код 9).

### Детонация (тик 61)
1. Урон магией: **1000 · (1 − dist/187.5)** по всем живым в радиусе **125** (граница включительно) — зона поражения 250×250 блоков. Кастер иммунен к своему взрыву.
2. Отброс от центра: сила `(1 − dist/125) · 12` + вверх 2.5; ослепление 100 тиков.
3. Кратер масштаба Иссушающей бури: ядро — взрывы BLOCK-силы 24 и 16 по земле + 4 кольца
   по 8 вторичных взрывов: r=16 (сила 10), r=32 (сила 8), r=48 (сила 8), r=64 (сила 6).
   Зона разрушения ~128 блоков.
4. Кастера сносит вниз — **замедленное падение спиной вниз**: старт со скорости −0.05,
   плавный разгон до потолка **−0.55** (−0.03/тик, ~11 блоков/с — быстрое, но кинематографичное
   слоу-мо);
   горизонтальная скорость давится вдвое каждый тик. Всё падение клиенты показывают позу
   падения на спине лицом вверх **с руками, вытянутыми вперёд от тела** (CombatAnimPacket,
   код 20, держится до посадки); камера показывает падение как **комикс из 4 панелей**
   (жёсткие склейки между ракурсами по 28 тиков: низкий геройский → профиль сбоку →
   кран сверху → широкий финал), а при касании земли сервер шлёт код 21 — комикс
   «перерисовывается в реальный майнкрафт»: обычная камера + поза лежания в кратере
   (85 тиков: распластался → медленно поднимается). Resistance 4 на 120 тиков.
5. **Иммунитет к падению**: 200 тиков (10 с) после детонации `fallDistance` кастера
   принудительно обнуляется каждый тик (включая всё слоу-мо падение) — приземление
   в кратер не наносит урона.
6. **Фрукт сгорает**: крылья снимаются, стойка сбрасывается в КУЛАКИ, силы отзываются (HP/резист), сообщение `rumblefruit.release_spent`.
7. **Ликвидация горы**: после взрыва сфера радиуса 64 вокруг точки удара стирается в воздух
   оболочками по 8 блоков за тик (8 тиков) — блоки удаляются (`Blocks.AIR`), неразрушимые
   (bedrock и т.п., destroySpeed < 0) выживают. Край оболочки светится (ELECTRO_GLOW).

### Дальнобойный визуал
Мега-колонна над кратером (высота 200) + 8 столбов кольцом r=32 (высота 128) и вспышка
взрыва шлются с флагом long-distance (force) каждому игроку — сервер доставляет их на
расстояние до **512 блоков** вместо ванильных 32. Тот же канал (`FarFx`) используют:
трансформация F (столб 120 + корона из 8 столбов), скилл V (столб 80 над целью),
спавн и удар метеорита (столбы 60 и 120).

### FX-канал (детерминирован при seed=42, yaw=37°)
Вся эмиссия частиц/звуков/молний воспроизводима побитово: контрольные хеши
потока на тиках 1, 4, 8, 10, 15, 30, 45, 60 и на взрыве зафиксированы в
`ReleaseSkillFxTest` — это и есть спецификация визуала.
**При касте скиллов используются ТОЛЬКО кастомные частицы и звуки мода**
(ELECTRO_SPARK / ELECTRO_GLOW / ELECTRO_CLOUD / ELECTRO_SLASH и звуки
rumblefruit:electro_*), ванильные — нет.

## 10. Пакеты (наблюдаемые клиентом)

| Событие | Пакет |
|---|---|
| Расход/перезарядка орба | OrbSyncPacket |
| Начало R | CombatAnimPacket(9) |
| Детонация R | CombatAnimPacket(20) — слоу-мо падение |
| Касание земли после J | CombatAnimPacket(21) — лежание в кратере |
| Смена стойки | StanceSyncPacket |
| Крылья вкл/выкл | WingsSyncPacket |

## 11. Метеориты и покои Экзорциста (`MeteorShower`, `MeteorDungeon`, `FallenExorcist`)

1. Метеорит падает **ровно раз в 20 минут** (24000 тиков) над случайным игроком;
   в HUD сверху по центру — обратный отсчёт «☄ Метеорит: MM:SS» (синк каждую секунду).
2. Команда **`/rumblefruit meteor`** призывает метеорит немедленно,
   **`/rumblefruit dungeon`** — прыжок в покои (отладка).
3. Метеорит — падающий блок магмы из точки на 55 блоков выше игрока (±40 по горизонтали),
   скорость −1.5; при спавне — вспышка + дальнобойный столб света (видно за километры),
   в полёте — огненный шлейф.
4. При ударе — кратер (взрыв силы 3) и в центре остаётся блок **Ядро метеорита**
   (светится, сверхпрочное); ударная вспышка бьёт столбом света вверх на 120 блоков.
5. **ПКМ по ядру** → игрока затягивает **в отдельное измерение `rumblefruit:exorcist_realm`**
   (пустота): там один раз на мир строятся **огромные покои** — зал 81×61×81 из
   блэкстоуна с золотым крестом арены, рёбрами из криющего обсидиана, глоустоун-сеткой
   света, колоннами и дайсом. Выдаётся электро-фрукт.
6. В центре покоев царит **Падший Экзорцист** — колосс **высотой 50 блоков**, собранный
   из сотен кубов (статуя: блэкстоун, золотая кираса, криющий обсидиан, нимб, кварцевые
   крылья архангела). 1500 HP, треть урона глушится панцирем, арсенал из **25 атак**
   (`ExorcistAttacks`, индекс зацикливается); ниже половины HP атакует вдвое чаще.
   Естественно не спавнится — только в покоях.
7. Выход — только через победу: при смерти Экзорциста все игроки в зале
   телепортируются обратно в своё измерение, туда откуда вошли.

---

### Инварианты (должны выполняться всегда)
- Орбов никогда не больше 4 и не меньше 0.
- Кастер никогда не получает урон от собственной R.
- После R ни один скилл не работает до поедания нового фрукта.
- Эмиссия эффектов детерминирована при фиксированном seed.
